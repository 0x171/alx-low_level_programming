#include <stdio.h>
/**
 * main - Print all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
*/
int main(void)
{
int trick;

for (trick = 48 ; trick <= 57 ; trick++)
{
putchar((char)trick);
}
putchar('\n');
return (0);
}
